node_modules/

# Local SQLite leftovers (legacy)
hackathon.db
hackathon.db-shm
hackathon.db-wal

# Uploaded artifacts (keep the dir via .gitkeep)
uploads/*
!uploads/.gitkeep

# Local env / secrets
.env
.env.*
!.env.example

# Editor / OS noise
*.bak
*.swp
.DS_Store

# Test artifacts
test.zip
coverage/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfvars
terraform/.terraform.lock.hcl
